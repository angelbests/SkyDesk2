use libloading::*;
pub fn loadhardware() {
    // let mut dllpath = std::env::current_dir().unwrap();
    // dllpath.push("lib\\CPPdll.dll");

    // let lib = unsafe { Library::new(dllpath).unwrap() };
    // let func: libloading::Symbol<unsafe extern "C" fn() -> String> =
    //     unsafe { lib.get(b"FetchLHMReport").unwrap() };
    // unsafe {
    //     func();
    // }
}
